// Modify the stats values here as needed
const totalUsers = 100000;
const activeExchanges = 200;
const supportedCoins = 30;
const customerSupport = "24/7";

// Update the stats values on the page
document.getElementById("total-users").textContent = totalUsers.toLocaleString();
document.getElementById("active-exchanges").textContent = activeExchanges.toLocaleString();
document.getElementById("supported-coins").textContent = supportedCoins.toLocaleString();
document.getElementById("customer-support").textContent = customerSupport;
